services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: library-postgres
      POSTGRES_USER_FILE: /run/secrets/db_user
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password
    secrets:
      - db_user
      - db_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U library_dev_user -d library-postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  setup:
    build:
      context: .
      dockerfile: setup/Dockerfile
    environment:
      POSTGRES_USER_FILE: /run/secrets/db_user
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password
      POSTGRES_DB: library-postgres
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      CSV_PATH: data-subset/google-books-subset.csv
      CHUNK_SIZE: 5000
    secrets:
      - db_user
      - db_password
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./setup/data:/app/data
    restart: "no"

  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    environment:
      FLASK_HOST: 0.0.0.0
      FLASK_PORT: 5000
      POSTGRES_DB: library-postgres
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      POSTGRES_USER_FILE: /run/secrets/db_user
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password
    secrets:
      - db_user
      - db_password
    depends_on:
      db:
        condition: service_healthy
      setup:
        condition: service_completed_successfully
    ports:
      - "5000:5000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/v1/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

secrets:
  db_user:
    file: ./secrets/db_user.txt
  db_password:
    file: ./secrets/db_password.txt

volumes:
  postgres_data:
