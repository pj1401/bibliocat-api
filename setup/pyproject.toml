[project]
name = "books-to-postgres"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "pandas>=3.0.2",
    "psycopg2-binary>=2.9.12",
    "sqlalchemy>=2.0.49",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
